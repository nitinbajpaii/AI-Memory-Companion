/* =============================================================
   Tailwind v4 configuration (CSS-first, referenced via @config)
   Custom design tokens are defined in index.css via @theme
   ============================================================= */

/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
};
